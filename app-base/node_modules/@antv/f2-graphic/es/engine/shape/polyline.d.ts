import Shape, { ShapeProp } from './shape';
import { PolylineAttrs } from '../../types';
export interface PolylineProp extends ShapeProp {
    attrs?: PolylineAttrs;
}
declare class Polyline extends Shape<PolylineProp> {
    _initProperties(): void;
    getDefaultAttrs(): {
        points: any;
        lineWidth: number;
        smooth: boolean;
    };
    createPath(context: any): void;
    calculateBox(): {
        minX: number;
        minY: number;
        maxX: any;
        maxY: any;
    };
}
export default Polyline;
