import Shape, { ShapeProp } from './shape';
import { PolygonAttrs } from '../../types';
export interface PolygonProp extends ShapeProp {
    attrs?: PolygonAttrs;
}
declare class Polygon extends Shape<PolygonProp> {
    _initProperties(): void;
    getDefaultAttrs(): {
        points: any;
        lineWidth: number;
    };
    createPath(context: any): void;
    calculateBox(): {
        minX: number;
        minY: number;
        maxX: any;
        maxY: any;
    };
}
export default Polygon;
