import Shape, { ShapeProp } from './shape';
import { MarkerAttrs } from '../../types';
export interface MarkerProp extends ShapeProp {
    attrs?: MarkerAttrs;
}
declare class Marker extends Shape<MarkerProp> {
    _initProperties(): void;
    getDefaultAttrs(): {
        x: number;
        y: number;
        lineWidth: number;
    };
    createPath(context: any): void;
    calculateBox(): {
        minX: number;
        minY: number;
        maxX: number;
        maxY: number;
    };
}
export default Marker;
