import Shape, { ShapeProp } from './shape';
export interface CustomProp extends ShapeProp {
    createPath?: Function;
    calculateBox?: Function;
}
declare class Custom extends Shape<CustomProp> {
    _initProperties(): void;
    createPath(context: any): void;
    calculateBox(): any;
}
export default Custom;
