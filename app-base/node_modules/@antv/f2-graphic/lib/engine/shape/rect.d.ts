import Shape, { ShapeProp } from './shape';
import { RectAttrs } from '../../types';
export interface RectProp extends ShapeProp {
    attrs?: RectAttrs;
}
declare class Rect<T extends RectProp = RectProp> extends Shape<T> {
    _initProperties(): void;
    getDefaultAttrs(): {
        x: number;
        y: number;
        width: number;
        height: number;
        radius: number;
        lineWidth: number;
    };
    createRadiusPath(context: any, x: any, y: any, width: any, height: any, radius: any): void;
    createPath(context: any): void;
    calculateBox(): {
        minX: number;
        minY: number;
        maxX: number;
        maxY: number;
    };
}
export default Rect;
