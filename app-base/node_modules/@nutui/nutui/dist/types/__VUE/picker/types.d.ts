import { App, PropType, CSSProperties } from 'vue';
declare type Install<T> = T & {
  install(app: App): void;
};
export interface PickerOption {
    text: string | number;
    value: string | number;
    disabled?: string;
    children?: PickerOption[];
    className?: string | number;
}
export interface PickerColumnOption {
    text: string;
    value: string;
    disabled: string;
    children: PickerOption[];
    className: string;
}
export declare type TouchParams = {
    startY: number;
    endY: number;
    startTime: number;
    endTime: number;
    lastY: number;
    lastTime: number;
};
